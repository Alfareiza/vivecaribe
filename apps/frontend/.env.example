# Copy to .env.local (not committed). Next.js loads env from this app root.

# Public API origin (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Default path after login (also used when callbackUrl is missing/invalid)
NEXT_PUBLIC_LOGIN_REDIRECT_URL=/reservas
