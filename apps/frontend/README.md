# ViveCaribe frontend

Next.js admin UI will live here. Not scaffolded yet — placeholder only
(see issue #36). Deploy contract for now: the Vercel container project remains
API-only (`Dockerfile.vercel` at repo root).
